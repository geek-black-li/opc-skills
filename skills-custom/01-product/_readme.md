# 自定义产品能力 Skill

存放个人原创或基于第三方深度改造的产品类 Skill，包括需求调研、需求分析、产品设计、原型和产品验收。

每个 Skill 使用 `skills-custom/01-product/<skill-id>/skill.yaml`，`skill-id` 必须使用 `zx-` 个人命名空间，推荐 `zx-product-*`；按需附带 `scripts/`、`references/` 或 `assets/`。不要在本说明目录直接放零散业务 YAML。
