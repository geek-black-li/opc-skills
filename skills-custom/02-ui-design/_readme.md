# 自定义 UI 设计能力 Skill

存放个人原创或基于第三方深度改造的 UI 设计类 Skill，包括界面设计、交互细节、设计规范、视觉交付和 UI 还原校验。

每个 Skill 使用 `skills-custom/02-ui-design/<skill-id>/skill.yaml`，`skill-id` 必须使用 `zx-` 个人命名空间，推荐 `zx-ui-*`；按需附带 `scripts/`、`references/` 或 `assets/`。不要在本说明目录直接放零散业务 YAML。
