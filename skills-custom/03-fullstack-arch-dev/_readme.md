# 自定义全栈与架构能力 Skill

存放个人原创或基于第三方深度改造的研发类 Skill，包括技术架构、方案设计、前后端开发、数据处理和系统集成。

每个 Skill 使用 `skills-custom/03-fullstack-arch-dev/<skill-id>/skill.yaml`，`skill-id` 必须使用 `zx-` 个人命名空间，推荐 `zx-dev-*`；按需附带 `scripts/`、`references/` 或 `assets/`。不要在本说明目录直接放零散业务 YAML。
