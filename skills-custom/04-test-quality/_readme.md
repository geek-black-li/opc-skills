# 自定义测试能力 Skill

存放个人原创或基于第三方深度改造的测试类 Skill，包括测试策略、计划、用例、自动化、质量评估和测试报告。

每个 Skill 使用 `skills-custom/04-test-quality/<skill-id>/skill.yaml`，`skill-id` 固定使用 `zx-test-<function>`，功能段优先一个通俗单词、最多两个；按需附带 `scripts/`、`references/` 或 `assets/`。不要在本说明目录直接放零散业务 YAML。
