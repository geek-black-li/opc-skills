# 自定义运维发布能力 Skill

存放个人原创或基于第三方深度改造的运维发布类 Skill，包括环境、构建、部署、发布、回滚、监控和迭代更新。

每个 Skill 使用 `skills-custom/05-ops-release/<skill-id>/skill.yaml`，`skill-id` 必须使用 `zx-` 个人命名空间，推荐 `zx-ops-*`；按需附带 `scripts/`、`references/` 或 `assets/`。不要在本说明目录直接放零散业务 YAML。
