# 自定义项目管理能力 Skill

存放个人原创或基于第三方深度改造的项目管理类 Skill，包括范围、计划、进度、风险、沟通、里程碑和交付管控。

每个 Skill 使用 `skills-custom/06-project-manage/<skill-id>/skill.yaml`，`skill-id` 必须使用 `zx-` 个人命名空间，推荐 `zx-project-*`；按需附带 `scripts/`、`references/` 或 `assets/`。不要在本说明目录直接放零散业务 YAML。
