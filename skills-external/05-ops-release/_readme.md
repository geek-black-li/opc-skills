# 第三方运维发布能力 Skill

存放已经完成来源、许可、兼容性和风险评估，并经用户确认原样入库的运维发布类第三方 Skill，包括环境、构建、部署、发布、回滚、监控和迭代更新。

每个 Skill 使用独立目录 `<skill-id>/`：可加载入口为 `skill.yaml`，第三方原件保存在 `source/` 且尽量不修改。待评估内容必须先进入 `skills-temp-inbox`。
